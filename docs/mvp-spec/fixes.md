# Fixes to Make

## 1. Bottom chat bar covers the MCP app interface

The Claude chat input bar overlaps the bottom of the MCP app UI, hiding content behind it. The widget layout must account for this so all content remains visible and scrollable above the chat bar.

## 2. Respect meals-per-day selection from onboarding survey

If the user selects 3 meals per day in the onboarding survey, the meal plan must generate and display exactly 3 meals per day (not 5). The number of meal rows in the Kanban board and the meals generated by the LLM must match the user's input.
